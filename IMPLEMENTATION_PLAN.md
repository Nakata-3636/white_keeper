# 白日焼け量評価 Webアプリ 実装計画

本計画は、仕様書 [spec.md](spec.md) に基づいて、現在の Streamlit 実装を段階的に拡張するための作業手順です。

## 1. 現状整理

- 係数計算の中核は [src/white_keeper/calculator.py](src/white_keeper/calculator.py) にあり、月別係数・時刻係数・天候係数・日焼け止め係数の積算はすでに実装済み。
- 保存処理は [src/white_keeper/storage.py](src/white_keeper/storage.py) で JSON ベースに実装済み。
- UI は [src/white_keeper/app.py](src/white_keeper/app.py) で動作しているが、現状は地域入力、外部天気API、自動補正、複数区間入力、評価表示の一部が未対応。
- テストは [tests/test_calculator.py](tests/test_calculator.py) にあり、計算ロジックと保存の基本動作のみを検証している。

## 2. 実装ゴール

1. 日付・地域・複数の外出区間・日焼け止め条件を入力できるようにする。
2. 外部天気APIから天候を自動取得し、取得不可時は手動選択に切り替える。
3. 仕様どおりの係数で日焼け量を算出し、スコア帯に応じた評価文を表示する。
4. 計算結果と入力履歴を保存し、後から確認できるようにする。
5. 主要ロジックに対するテストを追加し、仕様変更に強い構成にする。

## 3. 実装フェーズ

### フェーズ 1: ドメイン整理

- 計算ロジックを「時刻係数の取得」「区間ごとの積算」「スコア帯の判定」に分ける。
- 既存の単一区間計算を保ちつつ、複数区間を受け取れる関数を追加する。
- 天候・日焼け止め・評価ラベルの内部キーを列挙的に整理する。
- 入力値のバリデーションを共通化し、UI と計算層で同じルールを使う。

### フェーズ 2: 天候データ取得

- 地域入力から緯度経度または地区コードを得るための変換層を用意する。
- Open-Meteo などの外部APIを叩くクライアントを追加する。
- 過去日・当日・未来日で取得可否を分け、取得できない場合は手動入力にフォールバックする。
- API 失敗時の既定値、再試行、エラー表示の方針を決める。

### フェーズ 3: UI 拡張

- 日付入力に加え、都道府県と市区町村の入力方法を追加する。
- 外出時間を複数区間で追加・削除できる UI に変更する。
- 自動取得した天候を初期値として表示し、必要なら手動変更できるようにする。
- 計算結果に総スコア、係数内訳、評価メッセージを並べて表示する。
- 履歴表示に、地域・天候・評価ランクも含める。

### フェーズ 4: 永続化の拡張

- 保存レコードの構造を、単一区間前提から複数区間対応へ更新する。
- 天候の自動取得結果と手動修正の両方を保存できるようにする。
- 将来の集計や分析に備えて、保存データのバージョン情報を持たせる。

### フェーズ 5: テスト整備

- 係数テーブルの境界値テストを追加する。
- 複数区間の積算結果が仕様どおりになるかを検証する。
- 天候APIクライアントはモック可能な形にして、取得成功・失敗・手動フォールバックをテストする。
- 保存データの読み書きと更新動作を、複数レコード前提で確認する。

## 4. 実装順序

1. 計算ロジックの整理と複数区間対応。
2. スコア帯判定の追加。
3. 天候APIクライアントと地域変換の導入。
4. Streamlit UI の入力項目拡張。
5. 保存データ構造の更新。
6. テスト追加と回帰確認。

## 5. 受け入れ基準

- 仕様例どおりにスコアが計算できる。
- 天候が自動取得できない場合でも入力を完了できる。
- 複数区間入力でも計算結果が崩れない。
- 履歴が再表示でき、保存内容に欠損がない。
- 主要ロジックに対して pytest が通る。

## 6. リスクと対策

- 地域から緯度経度への変換が不安定な場合は、都道府県・市区町村の固定マスタを先に置く。
- 外部APIの仕様変更に備えて、API 層を計算層から完全に分離する。
- UI が複雑化しやすいため、入力と結果表示を別セクションに分けて整理する。
- 既存 JSON の形式変更で互換性が壊れないよう、読み込み時に旧形式を吸収する。

## 7. 次の着手点

- まず [src/white_keeper/calculator.py](src/white_keeper/calculator.py) に複数区間対応と評価判定を追加する。
- その後、天候取得クライアントと UI を接続する。
- 最後にテストを拡充して、仕様例と境界値を固定する。