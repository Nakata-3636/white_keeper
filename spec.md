# 日焼け量評価 Webアプリ 仕様書（改訂版 v2.0）

## 1. 概要
ユーザーが入力した「日付」「地域（都道府県・市区町村）」「外出時間」「日焼け止め使用状況」に基づき、外部天気APIから自動取得した天候データおよび科学的根拠（気象・皮膚科学）を取り入れた係数を算出して積算し、その日の「日焼け量（紫外線暴露スコア）」を自動計算・可視化するWebアプリケーション。

---

## 2. ユーザー入力項目 & データ取得フロー

### 2.1 ユーザー入力項目
1. **日付**（例: `2026-08-07`）
2. **対象地域**（例: `東京都文京区` または `都道府県 / 市区町村` のセレクトボックス）
   - ※位置情報（GPS）からの取得機能も選択可能とする。
3. **外出時間**
   - 開始時刻および終了時刻を入力（例: `10:00` 〜 `13:20`）
   - ※複数区間の入力に対応。
4. **日焼け止めの使用状況**
   - 「使用なし」「使用あり（塗り直しなし）」「使用あり（2〜3時間ごとに塗り直しあり）」から選択

### 2.2 外部API連携による天候の自動取得処理
- 入力された**日付**および**地域（緯度・経度 または 地区コード）**をキーとし、外部気象API（例: Open-Meteo Historical Weather API、気象庁API等）から過去/当日の天候データを自動取得する。
- **フォールバック処理（手動補正）**:
  - APIからの自動取得結果（例: 「晴れ」）を画面上にデフォルト表示しつつ、ユーザーが手動で変更（「曇り」「雨」等）できるようにする。
  - 未来の日付などデータが未取得の場合は、ユーザーに手動選択を求める。

---

## 3. 計算ロジック・各係数定義

### 基本計算式
1時間あたりの基本日焼け量を `1.0` とし、該当する時間帯ごとに各係数を掛け合わせた値を積算する。

$$ \text{総日焼け量スコア} = \sum_{t = \text{開始}}^{\text{終了}} \left( 1 \text{時間} \times \text{季節係数} \times \text{時刻係数}(t) \times \text{天候係数} \times \text{日焼け止め係数} \right) $$

※分単位の計算は、1分 = 1/60時間として按分計算を行う。

---

### 各係数の詳細

#### ① 季節係数（UV Index 月別変動基準）
| 月 | 係数 | 補足・根拠 |
| :--- | :--- | :--- |
| **12月, 1月** | `0.4` | 年間最小期（夏の約1/5） |
| **2月, 11月** | `0.6` | 紫外線増加開始・減退期 |
| **3月, 10月** | `1.0` | 基準値 |
| **4月, 9月** | `1.5` | 春先の急増期・残暑 |
| **5月, 8月** | `2.0` | ピーク前後の高線量期 |
| **6月, 7月** | `2.5` | 年間最大期（夏至前後） |

#### ② 時刻係数（太陽高度・時間帯別UV分布）
| 時間帯 | 係数 | 補足・根拠 |
| :--- | :--- | :--- |
| **00:00 - 04:59** | `0.0` | 日の出前 |
| **05:00 - 08:59** | `0.5` | 早朝（太陽高度低） |
| **09:00 - 09:59** | `1.0` | 午前（中程度） |
| **10:00 - 13:59** | `2.0` | **ピーク時間帯**（1日の約60%のUVが照射） |
| **14:00 - 14:59** | `1.0` | 午後（中程度） |
| **15:00 - 16:59** | `0.5` | 夕方 |
| **17:00 - 23:59** | `0.0` | 日没後 |

#### ③ 天候係数（APIデータのマッピング基準）
| 天候カテゴリ | 該当する気象状態（Weather Code例） | 係数 | 補足・根拠 |
| :--- | :--- | :--- | :--- |
| **晴れ** | 快晴、晴れ、ほぼ晴れ（全雲量 0〜30%） | `1.0` | 基準値 |
| **曇り** | うす曇り、時々曇り、薄雲（全雲量 30〜80%） | `0.8` | 紫外線の約80%が到達 |
| **厚い曇り** | どんより、本曇り（全雲量 80〜100%） | `0.6` | 紫外線の約60%が到達 |
| **雨・雪** | 弱雨〜強雨、雪、雷雨 | `0.3` | 厚い雲・雨天（UVの約20〜30%に減衰） |

#### ④ 日焼け止め係数（実効塗布量・保持効果）
| 日焼け止め条件 | 係数 | 補足・根拠 |
| :--- | :--- | :--- |
| **なし（塗っていない）** | `1.0` | 紫外線遮蔽効果なし |
| **あり（塗り直しなし）** | `0.3` | 平均的な塗布量（規定量の半分程度）での実効カット率（約70%カット） |
| **あり（2〜3時間ごとに塗り直し）** | `0.1` | 適切な塗り直しによる高い遮蔽保持（約90%カット） |

---

## 4. 計算例

**【設定】日付: 8月5日、地域: 東京都渋谷区、外出時間: 11:00〜14:30（3.5時間）、日焼け止め: あり（塗り直しなし）**

1. **APIデータ取得**: 8月5日の東京都渋谷区の11:00〜14:30の天候データをAPIから取得 ➔ 結果: 「晴れ」
2. **係数適用**:
   - 季節係数（8月）: `2.0`
   - 時刻係数:
     - 11:00〜13:59（3時間）: `2.0`
     - 14:00〜14:30（0.5時間）: `1.0`
   - 天候係数（晴れ）: `1.0`
   - 日焼け止め係数（あり）: `0.3`
3. **計算**:
   - 11:00〜14:00 (3h) のスコア: $3.0 \times (1.0 \times 2.0 \times 2.0 \times 1.0 \times 0.3) = 3.6$
   - 14:00〜14:30 (0.5h) のスコア: $0.5 \times (1.0 \times 2.0 \times 1.0 \times 1.0 \times 0.3) = 0.3$
   - **合計スコア**: $\mathbf{3.90}$

---

## 5. 出力・評価基準（スコア表示例）

算出された積算スコアに応じたフィードバック表示:

- **0.0 〜 1.9**: 【低】肌への影響は軽微です。
- **2.0 〜 4.9**: 【中】やや日焼けの可能性があります。長時間の外出時はケアを推奨します。
- **5.0 〜 8.9**: 【高】日焼けの危険性があります。冷却や保湿などのアフターケアを行ってください。
- **9.0 以上**: 【極めて高】強い紫外線ダメージを受けています。しっかりと肌を休ませてください。