# 日焼け量評価 Webアプリ 実装手順書（改訂版 v2.2）

## 1. 目的
本書は、日焼け量評価アプリを青を基調とした見た目に刷新し、外部天気APIからの自動取得、スコア判定、履歴保存、そして score_label に応じた色分けを実装するための作業手順書である。

---

## 2. 実装方針

### 2.1 UI方針
- 全体のテーマカラーを青系に統一する
- 背景は白〜淡い青で構成し、コントラストを保つ
- 見出しと主要アクションは濃い青で統一する
- 結果表示は card 型で視認性を高める
- `score_label` によって、結果カードの背景・文字色・枠線色を変化させる

### 2.2 判定色の方針
- 低: 緑系
- 中: 黄系
- 高: 赤系
- 極めて高: 赤系（より濃い赤）

### 2.3 画面の見え方
- 入力フォームは清潔な青系の設計
- 結果表示は大きく目立つ
- 判定に応じて、コールアウトカードの色が変わることで危険度がすぐ分かる

---

## 3. 実装対象ファイル

### 3.1 主要ファイル
- `src/white_keeper/app.py`
  - UIの構築
  - 天候取得の呼び出し
  - 判定色の適用
- `src/white_keeper/weather.py`
  - 天候取得ロジック
  - API失敗時フォールバック
- `src/white_keeper/calculator.py`
  - スコア計算
  - score_label の判定
- `data/records.json`
  - 履歴保存

### 3.2 補助ファイル
- `README.md`
  - 起動方法と説明
- `tests/test_calculator.py`
  - 回帰テスト

---

## 4. 実装ステップ

### ステップ1: UIの全体デザインを整える
対象: `src/white_keeper/app.py`

実施内容:
1. `st.set_page_config()` の設定を確認する
2. 全体の背景色を淡い青系に調整する
3. `st.title()` と `st.subheader()` を青系のアクセントカラーで統一する
4. 入力フォームの高さと余白を整える
5. ボタンを濃い青に設定する

実装方針:
- `st.markdown()` で CSS を埋め込んで、青基調の見た目を持つ
- 必要に応じて `unsafe_allow_html=True` を使ってスタイル調整を行う

---

### ステップ2: 結果表示のカードを変更する
対象: `src/white_keeper/app.py`

実施内容:
1. 計算結果を表示する箇所を `st.container()` で囲む
2. `score_label` を取得する
3. スコア帯ごとに色を分岐させる
4. 結果カードに以下を表示する
   - 総スコア
   - 判定ラベル
   - 判定メッセージ
   - 区間別内訳

条件:
- score_label == "低" → 緑
- score_label == "中" → 黄
- score_label == "高" → 赤
- score_label == "極めて高" → 赤（濃い赤）

---

### ステップ3: score_label に応じた色分けロジックを実装する
対象: `src/white_keeper/calculator.py` または `src/white_keeper/app.py`

実施内容:
1. `score_label` をキーにして色定義を作る
2. 色は辞書で管理する
3. UI表示時にその色を引き出して適用する

例:
```python
SCORE_COLORS = {
    "低": {"bg": "#E8F5E9", "text": "#2E7D32", "border": "#81C784"},
    "中": {"bg": "#FFF8E1", "text": "#F9A825", "border": "#FFD54F"},
    "高": {"bg": "#FFEBEE", "text": "#C62828", "border": "#EF5350"},
    "極めて高": {"bg": "#FFEBEE", "text": "#B71C1C", "border": "#E53935"},
}
```

---

### ステップ4: API取得結果をUIに反映する
対象: `src/white_keeper/app.py`

実施内容:
1. 地域入力フォームを追加する
2. 地域名を入力したときに `fetch_weather_suggestion()` を呼ぶ
3. 取得成功時は `weather_label` を初期値に設定する
4. 取得失敗時は `st.warning()` を表示し、`selectbox` で手動変更できるようにする

実装条件:
- API取得成功時: 自動取得メッセージを表示
- API取得失敗時: 手動選択メッセージを表示

---

### ステップ5: 手動選択と自動取得の両方を扱う
対象: `src/white_keeper/weather.py`, `src/white_keeper/app.py`

実施内容:
1. 自動取得の結果が `source = "api"` なら `weather_label` を使う
2. `source = "manual"` ならユーザーが選択した値を使う
3. `weather_label_to_key()` で天候キーへ変換する
4. 計算時には、最終的に決まった天候だけを使う

---

### ステップ6: 履歴表示を見やすくする
対象: `src/white_keeper/app.py`

実施内容:
1. 履歴一覧の行に `score_label` に応じた色を適用する
2. 合計値と評価ラベルが見やすいようにレイアウトする
3. 削除ボタンを青系のアクセントとし、視認性を保つ

---

### ステップ7: エラーメッセージと補足メッセージを統一する
対象: `src/white_keeper/app.py`

実施内容:
1. API失敗時は警告表示にする
2. 地域が見つからないときは明確なメッセージを表示する
3. 時間入力が不正なときはエラー表示を行う
4. すべての表示を青系・黄系・緑系・赤系の配色に整える

---

## 5. 実装順序
1. API取得ルートの接続
2. スコア判定の確認
3. 結果カードのUI化
4. `score_label` に応じたカラー分岐
5. 全体デザイン統一
6. 履歴表示整備
7. 例外メッセージ確認
8. テストの実行と調整

---

## 6. テスト方針

### 6.1 追加すべきテスト
- `score_label = "低"` のときに緑色系であること
- `score_label = "中"` のときに黄色系であること
- `score_label = "高"` のときに赤色系であること
- API取得失敗時に manual フォールバックになること
- 自動取得成功時に候補が `weather_label` に反映されること

### 6.2 実行コマンド
```bash
pytest -q
```

---

## 7. 完了条件
以下を満たした時点で実装完了とする。

- 青を基調とした見た目になっている
- `score_label` ごとに色が変わる
- API取得の成功/失敗が分かる
- 手動変更で補正できる
- 履歴表示が可能
- 既存の計算ロジックが壊れていない

---

## 8. まとめ
本実装では、機能面の正確さを保ちつつ、UIを青基調へ刷新し、結果の危険度が一目で分かるよう、`score_label` に応じた色分けを導入する。これにより、ユーザーは日焼けリスクを視覚的に理解しやすくなり、アプリ自体の見た目も洗練される。
